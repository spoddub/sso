# SSO

A reusable Single Sign-On service written in Go.

The project provides a basic authentication service that can be used as a shared auth component in other applications. It is built around gRPC, SQLite storage, JWT tokens, configuration files, migrations, and structured logging.

The service is intended to be simple, portable, and easy to integrate into other Go projects.
